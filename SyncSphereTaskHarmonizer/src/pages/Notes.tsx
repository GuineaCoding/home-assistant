import React from 'react';
import { IonPage, IonHeader, IonToolbar, IonTitle, IonContent } from '@ionic/react';

const Notes: React.FC = () => {
    return(
        <IonPage>
    <IonContent>Borea</IonContent>
    </IonPage>
    )
};

export default Notes;